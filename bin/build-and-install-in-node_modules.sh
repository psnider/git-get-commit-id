SOURCES='package.json git-get-commit-id.d.ts'
TARGET=node_modules/@sabbatical/git-get-commit-id
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./git-get-commit-id.js ${TARGET} 
tsc -p tsconfig.tests.json
